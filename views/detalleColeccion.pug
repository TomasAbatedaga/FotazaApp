extends layout

block contenido
  div(style="max-width: 1000px; margin: 40px auto; padding: 20px;")
    
    div(style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; border-bottom: 2px solid #eee; padding-bottom: 10px;")
      h2(style="color: #8a5a44; margin: 0;") 📁 Colección: #{coleccion.nombre}
      a(href="/colecciones" style="color: #666; text-decoration: none; font-weight: bold;")  Volver a mis colecciones

    if coleccion.publicaciones_guardadas && coleccion.publicaciones_guardadas.length > 0
      div.galeria(style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px;")
        each foto in coleccion.publicaciones_guardadas
          div.tarjeta-foto(style="background: #fff; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 5px rgba(0,0,0,0.1);")
            a(href=`/foto/${foto.id}` style="text-decoration: none; color: inherit;")
              div.contenedor-imagen
                if foto.imagenes && foto.imagenes.length > 0
                  img(src=foto.imagenes[0].url_imagen alt=foto.titulo style="width: 100%; height: 250px; object-fit: cover;")
              div.info-foto(style="padding: 15px;")
                h4(style="margin: 0 0 5px 0; color: #333;")= foto.titulo
                p(style="margin: 0; color: #8a5a44; font-weight: bold; font-size: 0.9em;") Por: #{foto.Usuario.nombre_usuario}
    else
      div(style="background: #fff; padding: 40px; border-radius: 8px; text-align: center; box-shadow: 0 2px 5px rgba(0,0,0,0.05);")
        h3(style="color: #666; margin-bottom: 15px;") Tu coleccion esta vacia
        p(style="color: #999; margin-bottom: 20px;") Todavia no guardaste ninguna foto aca.
        a.btn-oscuro(href="/" style="text-decoration: none; padding: 10px 20px; border-radius: 5px; background: #8a5a44; color: white;") Explorar fotos